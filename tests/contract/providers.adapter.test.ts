import { describe, it, expect } from 'vitest';

describe('Provider adapter contract (skeleton)', () => {
  it('retries on 429/5xx with backoff (placeholder)', async () => {
    expect(true).toBe(true);
  });
});
